# 数据与备份

Harness 会把项目状态、运行记录、错误日志和会话归档保存在本机用户数据目录中。自动测试使用独立目录，不会拿你的真实项目做测试。

最省心的备份方式：先退出 Harness，再完整复制用户数据目录。项目导出适合搬到另一台电脑，但不能替代源代码仓库和原始会话归档。

如果主状态文件损坏，程序会先保留一份带时间戳的副本，再尝试恢复，不会直接把坏文件覆盖掉。

请注意：“恢复项目”“拒绝结果”和“停止任务”都不会把源代码恢复到旧版本。恢复代码仍然要靠 Git 或其他备份。

日志不会保存密码或 token。把日志或导出包发给别人之前，仍建议先打开检查一遍。
